import React from 'react';
import Banner from '../container/home/Banner';

export default {
    component: Banner,
    title: 'ExoProperty/Banner',
};

const Template = (args) => <Banner {...args} />;

export const Primary = Template.bind({});